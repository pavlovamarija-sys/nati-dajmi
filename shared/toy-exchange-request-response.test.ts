// @ts-ignore Deno requires explicit extensions.
import { parseExchangeRequestMutationResponse, parseMyExchangeRequestsResponse } from './toy-exchange-request-response.ts';
declare const Deno:{test(name:string,fn:()=>void):void};
Deno.test('strictly parses safe mutation and read DTOs',()=>{assertEqual(parseExchangeRequestMutationResponse({request:mutation()}).status,'PENDING');const result=parseMyExchangeRequestsResponse({sent:[card()],received:[]});assertEqual(result.sent[0].listingName,'Камион');});
Deno.test('rejects private fields and malformed lifecycle',()=>{assertThrows(()=>parseExchangeRequestMutationResponse({request:{...mutation(),owner_user_id:'owner'}}));assertThrows(()=>parseMyExchangeRequestsResponse({sent:[card({listingImageUrl:'javascript:bad'})],received:[]}));assertThrows(()=>parseExchangeRequestMutationResponse({request:mutation({status:'ACCEPTED',respondedAt:null})}));});
function mutation(overrides:Record<string,unknown>={}){return{requestId:'request-1',listingId:'listing-1',status:'PENDING',createdAt:'2026-08-21T10:00:00Z',respondedAt:null,...overrides};}function card(overrides:Record<string,unknown>={}){return{...mutation(),listingName:'Камион',askingValueStars:500,listingImageUrl:'https://example.supabase.co/signed.jpg',...overrides};}function assertEqual(a:unknown,e:unknown){if(!Object.is(a,e))throw new Error('Assertion failed.');}function assertThrows(fn:()=>void){try{fn();}catch{return;}throw new Error('Expected throw.');}
